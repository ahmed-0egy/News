<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/bootstrap.css">
    <link rel="stylesheet" href="css/news.css">
    <title>Document</title>
</head>

<body>
    <main>
        {{>header}}

        <section class="row m-5">
            {{#each returnValue}}
            <div class="container-card col-lg-6 col-xl-4 col-xxl-3">
                <div class="body-card card h-100">
                    <img src={{this.urlToImage}} class="card-img-top" alt="Event Image" onerror="handleError(this);">
                    <div class=" card-body">
                        <h5 class="card-title">{{this.title}}</h5>
                        <h5 class="card-title" style="color:red;">By {{this.author}}</h5>
                        <p class="card-text">{{this.description}}</p>
                    </div>
                </div>
            </div>
            {{/each}}
        </section>
    </main>

    <script src="js/news.js"></script>
</body>

</html>